name: fresnel_zone_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.6.0 <4.0.0'
  flutter: '>=3.29.0 <3.30.0'
dependencies:
  flutter:
    sdk: flutter
  
  provider: 6.1.5
  file_picker: 10.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
